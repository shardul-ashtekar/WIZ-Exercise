name: WIZ Terraform Deploy

on:
  push:
    branches:
      - main
      - feature/*

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
      AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        run: terraform init -backend-config=env/dev/backend_override.tfvars
        working-directory: tf/

      - name: Terraform Plan
        run: terraform plan -var-file=env/dev/terraform.tfvars
        working-directory: tf/

      - name: Terraform Apply
        run: terraform apply -auto-approve -var-file=env/dev/terraform.tfvars
        working-directory: tf/
